---
import { Tweet as TweetEmbed } from "astro-embed";

interface Props {
  url: string;
  theme: "light" | "dark" | undefined;
}

const { url, theme = "dark" } = Astro.props;
---

<TweetEmbed id={url} theme={theme} />

<script async src="https://platform.twitter.com/widgets.js"></script>
